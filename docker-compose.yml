services:
  frontend:
    build: .
    working_dir: /app
    ports:
      - "3000:3000"
    restart: always
    volumes:
      - .:/app:Z
      - app_node_modules:/app/node_modules
    
volumes:
  app_node_modules: